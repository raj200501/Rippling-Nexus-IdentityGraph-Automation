version: '3.9'
services:
  api:
    image: python:3.11
    command: bash -lc "pip install -e .[dev] && uvicorn apps.api.nexus_api.main:app --host 0.0.0.0 --port 8000"
  web:
    image: node:20
    command: bash -lc "cd apps/web && npm install && npm run dev -- --host 0.0.0.0 --port 5173"
